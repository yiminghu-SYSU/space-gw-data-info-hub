# 修改与维护说明

[返回总目录](README.md)

本仓库向重点研发专项参与人员开放协作。大多数更新可以直接在 GitHub 网页中完成，不需要安装 Git，也不要求会编程。

## 最简单的修改方法

适合修改错别字、日期、链接或补充少量内容。

1. 在 GitHub 中打开需要修改的 Markdown 文件；
2. 点击页面右上方的铅笔图标 **Edit this file**；
3. 修改正文；
4. 点击 **Preview** 检查排版和链接；
5. 点击 **Commit changes**；
6. 在提交说明中简短写明改了什么，例如“更新 NUS 博后截止日期”。

拥有写入权限的成员可以直接提交。若对修改没有把握，可以选择新建分支并发起 Pull Request，请其他成员确认后合并。

## 添加一条新信息

1. 进入对应目录：`招聘`、`会议`、`在线学术报告` 或 `通知`；
2. 点击 **Add file → Create new file**；
3. 按 `日期-简短标题.md` 命名，例如 `2026-11-15-nbi-postdoc.md`；
4. 复制[通用信息模板](模板/通用信息模板.md)的内容；
5. 在顶部填写中文摘要；
6. 在“完整通知原文”部分粘贴完整邮件或公告；
7. 保存后，再编辑该目录的 `README.md`，把新文件加入当前信息列表；
8. 如果信息很重要，再把它加入总目录 `README.md` 的“最近更新”。

## 招聘和会议信息的推荐格式

每条信息建议同时包含两部分：

1. **简要信息**：机构、职位或活动名称、截止日期、地点、联系方式和原始链接；
2. **完整通知原文**：保留收到的完整英文或中文通知，方便读者直接获取全部细节。

不必花时间重写完整通知，也不要只留下没有上下文的转发截图。

## 文件整理规则

- 日期统一写成 `YYYY-MM-DD`；
- 截止日期不明确时写“待确认”，不要猜测；
- 已过期信息继续保留，但从“当前信息”移到“已过期”；
- 修改他人发布的信息时，在提交说明中写清原因；
- 尽量引用招聘机构、会议主办方或合作组织的原始链接；
- 同一条信息只创建一个文件，后续更新直接修改原文件。

## 公开前检查

如果仓库将设为公开，请不要提交：

- 账号、密码、验证码和访问令牌；
- 未公开的线上会议链接或会议口令；
- 内部服务器配置和计算资源账号；
- 未经同意的个人手机号、私人邮箱或其他个人信息；
- 明确标注不得外传的邮件、文件和讨论内容。

遇到不确定的信息，可以先只在目录 `README.md` 中写标题并标记“待核对”，确认后再补充完整内容。
